import Header from "layouts/header";
import { FC } from "react";

const Main: FC = () => (
	<div className="w-screen h-screen overflow-scroll overflow-x-hidden bg-slate-900 text-white">
		<Header />
	</div>
);
export default Main;
